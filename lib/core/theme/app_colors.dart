import 'package:flutter/material.dart';

class AppColors {
  // 🎨 الألوان الأساسية للتطبيق
  static const primary = Color.fromARGB(255, 135, 154, 230);
  static const textDark = Color(0xFF333333);
  static const textLight = Color(0xFF757575);
 
 
  // 🎨 ألوان الصيام
  static const obligatoryFasting = Color(0xFF4CAF50);
  static const recommendedFasting = Color(0xFF2196F3);
  static const forbiddenFasting = Color(0xFFE53935);
  static const normalDay = Color(0xFFE0E0E0);

  // 🎨 ألوان إضافية
  static const highlightDay = Color.fromRGBO(225, 225, 225, 1);
  static const selectedDay = Color(0xFFD1C4E9);
}
